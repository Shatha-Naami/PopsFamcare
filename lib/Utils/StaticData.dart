import 'package:flutter/material.dart';
import 'package:famcare/main.dart';

double screenWidth = MediaQuery.of(key.currentContext).size.width;
double screenHeight = MediaQuery.of(key.currentContext).size.height;

TextStyle commonStyle = TextStyle(color: Colors.black, fontSize: 16);
